# Практична частина: кластерна модель хоризматмутази (ORCA)

> Самодостатнє ТЗ для окремого чату. Тут є весь потрібний контекст — попередню розмову читати не треба.

---

## 0. Контекст і мета (прочитай перед початком)

**Тема доповіді:** стабілізація перехідного стану як джерело каталітичної сили ферментів.
**Модельна система:** хоризматмутаза *Bacillus subtilis* (BsCM) — каталізує [3,3]-сигматропне перегрупування Кляйзена хоризмату → префенат через крісло-подібний перициклічний перехідний стан. Реакція йде і без ферменту, ковалентного каталізу та кофакторів немає — тому система ізолює **чисту нековалентну (переважно електростатичну) стабілізацію TS**. Це і робить її ідеальним обчислювальним тестом теорії.

**Що треба порахувати (три величини ΔG‡ і два порівняння):**
1. `ΔG‡(water)` — некаталізована реакція (лише субстрат у континуумі води).
2. `ΔG‡(WT)` — кластер активного центру дикого типу (Arg90).
3. `ΔG‡(Arg90Cit)` — той самий кластер, але каталітичний Arg90 замінено на нейтральний ізостерний цитрулін.

**Головні висновки, які має дати розрахунок:**
- `ΔΔG‡_кат = ΔG‡(water) − ΔG‡(WT) > 0` — фермент знижує бар'єр.
- `ΔG‡(Arg90Cit) − ΔG‡(WT) > 0` — зняття одного позитивного заряду підіймає бар'єр (in silico відтворення експерименту Arg90→Cit, який дає падіння k_cat > 10⁴). Це «грошовий» результат: він показує, що каталіз тут електростатичний.

**Метод:** кластерна модель у стилі Himo (обрізаний активний центр + континуум-розчинник + заморожені якірні атоми). Відтворюємо методику Burschowsky et al., *FEBS Open Bio* 7 (2017), але в **ORCA 6.x** (оригінал був у Gaussian09).

**Залізо:** ~100–170 атомів, B3LYP/6-31G(d) — недорого; орендований багатоядерний вузол на кілька годин достатньо.

---

## 1. Інструменти

- **ORCA 6.1.x** — оптимізації, TS, частоти, single-points.
- **xTB / GFN2-xTB** (є всередині ORCA як `! XTB2`) — дешева передоптимізація й preopt для NEB.
- **PyMOL** — інспекція структури, вибір залишків, вимір H-зв'язків, рендер активного центру для слайдів.
- **Avogadro2 / Molden / Chemcraft** — побудова/капінг кластера, перегляд і анімація уявної моди.
- **NCIPLOT 2.0** (опційно) — аналіз нековалентних взаємодій, як у Burschowsky (для тримінгу кластера й для картинки в доповідь).
- **Python + matplotlib** — енергетична діаграма й таблиці.

---

## 2. Вхідні структури

- **3ZP7** — високороздільний комплекс BsCM Arg90Cit із субстратом/продуктом. Основна відправна точка. Качати з RCSB: https://www.rcsb.org/structure/3ZP7
- **WT-структура** (Arg90) — звірити PDB-ID у Burschowsky ref [40] / у статті PNAS 2014 «Electrostatic transition state stabilization rather than reactant destabilization…» (є в теці 42). Для WT-кластера потрібні координати гуанідинію Arg90.
- Обидві структури — це кристалографічні знімки; реакційний шлях (R→TS→P) будуємо розрахунком.

---

## 3. Побудова кластера

**3.1. Відбір залишків.** Беремо реакційний центр (субстрат) + перше координаційне оточення — усе, що дає H-зв'язки / електростатику з двома карбоксилатами й ефірним киснем субстрату. За Burschowsky це **7 залишків**:

`Arg7, Arg63, Thr74, Cys75, Glu78, Arg90 (WT) / Cit90 (варіант), Tyr108`

плюс **одна структурна молекула води**, що розділяє негативні заряди під час реакції (не викидати — вона впливає на механізм).

Спосіб відбору: за методикою Sevastik & Himo; для тримінгу зайвого (каркас, неконтактні метили) прогнати NCIPLOT і прибрати атоми, що не дають нековалентних контактів.

**3.2. Обрізання й капінг.** Кожен залишок урізаємо до функціональної бічної групи, розрив насичуємо воднем:
- Arg → гуанідиній (H₂N–C(=NH₂⁺)–NH–…, обрізаний до метил/етилгуанідинію)
- Glu → ацетат/пропіонат (карбоксилат)
- Tyr → п-крезол / фенол
- Cys → метантіол
- Thr → малий спирт (гідроксил + метил)
- **residue 90:** WT — гуанідиній (NH₂⁺-група); Cit — нейтральна сечовинна група (за Burschowsky як нейтральний зонд достатньо замінити катіонну NH₂⁺ на =O).

**3.3. Заморожування (ключове для кластерної моделі).** Термінальні (капінгові / якірні) атоми **фіксуємо в кристалографічних координатах** — це імітує жорсткість білкового каркаса й не дає бічним ланцюгам «сповзати» в позиції сусідніх залишків. Практично: заморожуємо капінгові H (і атом, до якого вони приєднані) кожного обрізаного залишку. Список 0-based індексів цих атомів знадобиться в кожному інпуті (див. §5).

> **Однакове впорядкування атомів** у R, TS, P і в обох варіантах (WT/Cit) обов'язкове — інакше поламаються і NEB, і перевикористання індексів констрейнтів. Продукт будувати редагуванням геометрії реактанта (розрив C–O ефірного зв'язку енолпіруватної частини + утворення нового C–C), а не з нуля.

---

## 4. Заряд і мультиплетність (перевірити перед кожним запуском!)

Це найчастіше джерело сміттєвого бар'єра. Порахувати руками по своєму фактичному кластеру. Очікувані значення:

| Компонент | Заряд |
|---|---|
| Хоризмат / префенат (діаніон) | −2 |
| Arg7, Arg63 (гуанідиній ×2) | +2 |
| Arg90 (WT) | +1 |
| Glu78 (карбоксилат) | −1 |
| Cys75, Thr74, Tyr108, H₂O (нейтральні) | 0 |
| **Разом WT-кластер** | **0** |
| **Разом Arg90Cit** (замість +1 → 0) | **−1** |

Мультиплетність = **1** (закрита оболонка, синглет) для всіх станів. Заряд зберігається R→TS→P (префенат теж −2).

---

## 5. Воркфлоу і готові інпути ORCA

Порядок: (A) передоптимізація xTB → (B) опт+частоти R і P на DFT → (C) TS через NEB-TS + частоти → (D) high-basis single-points зі скануванням ε → (E) референсна реакція у воді → (F) варіант Arg90Cit → (G) аналіз і візуалізація.

### 5.A. Дешева передоптимізація (xTB)
```
! XTB2 Opt
%geom
  Constraints
    {C 5 C}   # приклад: 0-based індекси заморожених капінгових атомів
    {C 12 C}
    # ... по одному рядку на кожен заморожений атом
  end
end
%pal nprocs 16 end
* xyzfile 0 1 react_wt.xyz
```

### 5.B. Опт + частоти реактанта і продукту (DFT, кластер)
```
! B3LYP D3BJ 6-31G(d) Opt Freq CPCM TightSCF
%cpcm epsilon 4.0 refrac 1.4 end   # ε≈4 ~ внутрішність білка
%geom
  Constraints
    {C 5 C}
    {C 12 C}
    # ... ті самі індекси, що й скрізь
  end
end
%maxcore 3000
%pal nprocs 16 end
* xyzfile 0 1 react_wt.xyz
```
Те саме для `prod_wt.xyz`. Очікувано: після опту із замороженими атомами буде **~10 дрібних уявних частот** — це артефакт констрейнтів, не плутати з реакційною модою.

### 5.C. Пошук TS (NEB-TS між оптимізованими R і P)
```
! B3LYP D3BJ 6-31G(d) NEB-TS Freq CPCM TightSCF
%cpcm epsilon 4.0 refrac 1.4 end
%neb
  NEB_END_XYZFILE "prod_wt.xyz"
  NImages 8
  Preopt_Ends true
end
%geom
  Constraints
    {C 5 C}
    {C 12 C}
    # ...
  end
end
%maxcore 3000
%pal nprocs 16 end
* xyzfile 0 1 react_wt.xyz
```
**Критерій успіху TS:** одна велика уявна частота (сотні i·см⁻¹) з рухом уздовж розриву C–O / утворення C–C — поверх тих ~10 дрібних від констрейнтів. Перевірити анімацією моди.
Альтернатива, ближча до Burschowsky: guess TS із реактанта → `! OptTS` з `%geom Calc_Hess true end`, далі за бажанням IRC (перший прогін крок 0.001 Bohr, щоб розслабити заморожені метили, потім 0.01 Bohr).

### 5.D. High-basis single-points + скан ε (для кожного з R/TS/P)
```
! B3LYP D3BJ 6-311+G(d,p) CPCM TightSCF
%cpcm epsilon 4.0 refrac 1.4 end
%maxcore 3000
%pal nprocs 16 end
* xyzfile 0 1 ts_wt.xyz
```
Повторити з `epsilon` = 1, 2, 4, 20, 40 (як у Burschowsky — перевірка чутливості до діелектрика).

**Вільна енергія:** `G = E(SP, 6-311+G**, ε) + [G − E_el]_opt`, де корекцію `[G − E_el]` беремо з опт/частотного розрахунку (§5.B). У виводі ORCA це «Final Gibbs free energy» мінус «Electronic energy». Через заморожені атоми ентропійна частина наближена — це прийнятне обмеження методу (так само в оригіналі).

### 5.E. Референсна (некаталізована) реакція — субстрат сам у воді
```
! B3LYP D3BJ 6-31G(d) Opt Freq CPCM(water) TightSCF
%maxcore 3000
%pal nprocs 16 end
* xyzfile -2 1 chorismate.xyz    # діаніон, без залишків, без констрейнтів
```
Далі TS і продукт так само (NEB-TS без констрейнтів), потім high-basis SP у воді (ε=78.4). `ΔΔG‡_кат = ΔG‡(water) − ΔG‡(WT)`.

### 5.F. Варіант Arg90Cit
Той самий кластер, residue 90 → нейтральна група, **charge = −1**:
```
* xyzfile -1 1 react_cit.xyz
```
Прогнати R/P опт+freq, TS (NEB-TS), high-basis SP — усе як у 5.B–5.D. Порівняти `ΔG‡(Arg90Cit)` з `ΔG‡(WT)`.

---

## 6. Аналіз (опційно, але сильно для доповіді)

- **Електричне поле на реакційному зв'язку** — у дусі Boxer (KSI) і Warshel «catalytic field»: оцінити поле, яке кластер створює на C–O/C–C, і як воно падає в Arg90Cit. Пов'язує твій розрахунок із загальнотеоретичною частиною.
- **NCIPLOT** — карта нековалентних взаємодій субстрат↔залишки (та сама, що в Burschowsky) — гарна ілюстрація ролі Arg90.
- **Внесок окремих залишків** — за бажанням: прибирати/нейтралізувати по одному й дивитися ΔΔG‡.

---

## 7. Візуалізація і результати на виході

**Візуалізація:**
- PyMOL: рендер активного центру (субстрат + 7 залишків + вода, H-зв'язки пунктиром) — для слайда й для визначення індексів заморожених атомів.
- `orca_pltvib ts_wt.hess 1` → траєкторія уявної моди; переглянути в Avogadro/Molden/Chemcraft.
- matplotlib: одна діаграма з трьома профілями R→TS→P — вода / WT / Arg90Cit.

**Фінальні артефакти:**
1. Таблиця `ΔG‡`: water, WT, Arg90Cit (+ скан ε).
2. `ΔΔG‡_кат` і `ΔΔG‡(Cit−WT)` з коротким висновком.
3. Рисунок активного центру (PyMOL).
4. Анімація/скрін уявної моди TS.
5. Енергетична діаграма (3 криві).
6. Усі `.xyz` фінальних геометрій (R/TS/P × water/WT/Cit) + лог-файли.

---

## 8. Типові пастки

- **Заряд** — перерахувати по фактичному кластеру щоразу (§4). Помилка = безглуздий бар'єр.
- **Уявні частоти від констрейнтів** (~10 дрібних) — норма; реакційна мода одна й велика. Не панікувати.
- **Термохімія із замороженими атомами** — наближена; використовувати `G = E_SP + G_corr`, ентропію трактувати обережно.
- **Однакове впорядкування атомів** і **однаковий набір констрейнтів** у R/TS/P/WT/Cit — інакше NEB і порівняння енергій некоректні.
- **Дисперсія** (D3BJ) — вплив малий (~1 кДж/моль у Burschowsky), але лишаємо для коректності.
- **def2-базиси** як сучасна альтернатива Pople (def2-SVP опт / def2-TZVP SP) — можна, але тоді втрачається пряма числова порівнюваність із Burschowsky; для відтворення тримайся 6-31G(d) / 6-311+G(d,p).

---

## 9. Джерело методики

Burschowsky, van der Kamp, Balcells et al. «Quantum chemical modeling of the reaction path of chorismate mutase based on the experimental substrate/product complex», *FEBS Open Bio* 7 (2017). Структура: PDB 3ZP7. Експериментальна опора: PNAS 2014 (Arg90→Cit, електростатична стабілізація TS). Обидві є у теці `42/`.
